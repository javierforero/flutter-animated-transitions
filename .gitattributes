* text eol=lf

# Make sure that these Windows files always have CRLF line endings in checkout
*.bat    text eol=crlf
*.ps1    text eol=crlf

# Never perform LF normalization on these files
*.ico    binary
*.jar    binary
*.png    binary

# fonts
*.ttf    -text diff

# nicely merge project configs
*.pbxproj merge=union